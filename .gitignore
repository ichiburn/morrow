# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.coverage
htmlcov/

# シークレット
.env
.env.*
!.env.example
*.pem
*.key
auth.json

# MORROW: 実験の実行時状態（evaluator ドメイン。公開しない）
.morrow/
out/

# Foundry: `foundryctl forge` が casting.yaml.lock から生成する配備ファイル。
# lock がコミットされていれば誰でも再生成できるので、生成物は追跡しない。
# 先頭 / でリポジトリ直下に限定する（下位階層の同名ディレクトリを巻き込まないため）。
/pours/

# MORROW: 設計の反復履歴（ローカルの作業記録。公開リポジトリには含めない）
docs/_history/

# ローカル開発ハーネス（claude-code-template 由来。MORROW の成果物ではない）
prompts/
templates/
crates/
scripts/
memory/
.agents/
.codex/
.gemini/
.claude/
.mcp.json
.gitmodules
AGENTS.md.template
GEMINI.md
docs/analysis-cache/
docs/handoff/
docs/plans/
docs/tasks/
docs/infra/
docs/development/
docs/testing/

# エディタ・OS
.DS_Store
.idea
.vscode

# ローカル環境のシェル設定（開発ホスト固有。成果物ではない）
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc
.ai-results/
.needs-init
.auto-init-result.json
.dep-check-result.json
.trash/
